apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatbot-frontend
  namespace: chatbot

spec:
  replicas: 1

  selector:
    matchLabels:
      app: chatbot-frontend

  template:
    metadata:
      labels:
        app: chatbot-frontend

    spec:

      containers:

      - name: frontend

        image: rahulloganathan/chatbot-frontend:259b6d6b25402c50842ceed75f03a78aa3cb6bbf

        imagePullPolicy: Always

        ports:
        - containerPort: 8501

        # env:
        # - name: BACKEND_URL
        #   value: "http://chatbot-backend:8000/chat"

        resources:

          requests:
            cpu: "200m"
            memory: "256Mi"

          limits:
            cpu: "500m"
            memory: "512Mi"
