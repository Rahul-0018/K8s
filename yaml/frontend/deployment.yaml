apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatbot-frontend
  namespace: chatbot

spec:
  replicas: 1

  selector:
    matchLabels:
      app: chatbot-frontend

  template:
    metadata:
      labels:
        app: chatbot-frontend

    spec:

      containers:

      - name: frontend

        image: rahulloganathan/chatbot-frontend:159137b1091ea842968da27d8747f8fb0850fd7d

        imagePullPolicy: Always

        ports:
        - containerPort: 8501

        # env:
        # - name: BACKEND_URL
        #   value: "http://chatbot-backend:8000/chat"

        resources:

          requests:
            cpu: "200m"
            memory: "256Mi"

          limits:
            cpu: "500m"
            memory: "512Mi"
