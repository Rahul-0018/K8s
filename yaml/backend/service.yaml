apiVersion: v1
kind: Service
metadata:
  name: chatbot-backend
  namespace: chatbot

spec:

  selector:
    app: chatbot-backend

  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP

  type: ClusterIP