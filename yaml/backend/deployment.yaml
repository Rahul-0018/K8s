apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatbot-backend
  namespace: chatbot
spec:
  replicas: 1

  selector:
    matchLabels:
      app: chatbot-backend

  template:
    metadata:
      labels:
        app: chatbot-backend

    spec:
      containers:

      - name: backend

        image: rahulloganathan/chatbot-backend:78c887a3d4bf2b86a9e713a77fa74f500187718b

        imagePullPolicy: Always

        ports:
        - containerPort: 8000

        env:
        - name: GROQ_API_KEY
          valueFrom:
            secretKeyRef:
              name: groq-secret
              key: GROQ_API_KEY

        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"

          limits:
            cpu: "500m"
            memory: "512Mi"
