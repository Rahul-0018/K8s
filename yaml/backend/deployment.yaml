apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatbot-backend
  namespace: chatbot
spec:
  replicas: 1

  selector:
    matchLabels:
      app: chatbot-backend

  template:
    metadata:
      labels:
        app: chatbot-backend

    spec:
      containers:

      - name: backend

        image: rahulloganathan/chatbot-backend:d018ee9fe97affbbbd1c7858d86be81e08b4982d

        imagePullPolicy: Always

        ports:
        - containerPort: 8000

        env:
        - name: GROQ_API_KEY
          valueFrom:
            secretKeyRef:
              name: groq-secret
              key: api-key

        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"

          limits:
            cpu: "500m"
            memory: "512Mi"
